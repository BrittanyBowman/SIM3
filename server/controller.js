const bcrypt = require('bcrypt');

module.exports = {
    login: (req, res) => {
        let {username, password} = req.body;

    },
    register: (req, res) => {
        let {username, password} = req.body;

    }
}